<% content_for :title, 'Niños - TIC' %>
<% content_for :page_title, 'Niños' %>
<% content_for :page_breadcrumb, "<li>#{ link_to '<span class="fa fa-dashboard"></span> Dashboard'.html_safe, root_path }</li><li>Niños</li>".html_safe %>

<div class='box box-solid'>
  <div class='box-header with-border'>
    <h3 class='box-title'>Actions</h3>
  </div>
  <div class='box-body'>
    <%= link_to '<i class="fa fa-plus"></i> Inscribir'.html_safe, new_child_path, class: 'btn btn-app' %>
  </div>
</div>

<div class='box box-solid'>
  <div class='container-fluid table-responsive'>
    <table class='table table-bordered table-hover dataTable responsive no-wrap' id='children-table', data-source="<%= children_path(format: :json) %>">
      <br>
      <thead>
        <tr>
          <th>Control</th>
          <th>Name</th>
          <th>Last name</th>
          <th>Gender</th>
          <th>Birthdate</th>
        </tr>
      </thead>
    </table>
  </div>
</div>