<% content_for :title, 'Coordinadores - TIC' %>
<% content_for :page_title, 'Coordinadores' %>
<% content_for :page_breadcrumb, "<li>#{ link_to '<span class="fa fa-dashboard"></span> Dashboard'.html_safe, root_path }</li><li>Coordinadores</li>".html_safe %>

<div class='box box-solid'>
  <div class='box-header with-border'>
    <h3 class='box-title'>Acciones</h3>
  </div>
  <div class='box-body'>
    <%= link_to '<i class="fa fa-plus"></i> Agregar'.html_safe, new_coordinator_users_path, class: 'btn btn-app' %>
  </div>
</div>

<div class='box box-solid'>
  <div class='container-fluid table-responsive'>
    <table class='table table-bordered table-hover dataTable responsive no-wrap' id='coordinators-table', data-source="<%= coordinators_users_path(format: :json) %>">
      <br>
      <thead>
        <tr>
          <th>Control</th>
          <th>Name</th>
          <th>Last name</th>
          <th>Gender</th>
          <th>Email</th>
          <th>Birthdate</th>
        </tr>
      </thead>
    </table>
  </div>
</div>