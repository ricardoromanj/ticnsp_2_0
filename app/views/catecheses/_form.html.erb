<%= form_for(@catechese) do |f| %>
  <% if @catechese.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@catechese.errors.count, "error") %> prohibited this catechese from being saved:</h2>

      <ul>
      <% @catechese.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image_id %><br>
    <%= f.text_field :image_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
