<% content_for :title, 'Coordinación - TIC' %>
<% content_for :page_title, 'Coordinación' %>

<div class='box box-solid'>
  <div class='box-header with-border'>
    <h3 class='box-title'>Información de contacto</h3>
  </div>
  <div class='box-body'>

    <table class='table table-responsive table-hover'>
      <thead>
        <th></th>
        <th>Nombre</th>
        <th>Correo electrónico</th>
      </thead>
      <tbody>
        <% @staff.each do |user| %>
          <tr>
            <td><%= image_tag user.current_image(50), class: 'pull-right img-circle' %></td>
            <td><%= user.full_name %><br><strong><%= user.usertype_string %></strong></td>
            <td><%= link_to user.email, "mailto:#{user.email}" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

  </div>
</div>
